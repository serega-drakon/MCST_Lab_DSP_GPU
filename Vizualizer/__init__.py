import matplotlib.pyplot as plt
import numpy as np

plt.style.use('_mpl-gallery-nogrid')



# make data
#X, Y = np.meshgrid(np.linspace(-n, n, 16), np.linspace(-n, n, 16))
#Z = (1 - X/2 + X**5 + Y**3) * np.exp(-X**2 - Y**2)

# plot
#fig, ax = plt.subplots()

#ax.imshow(Z, cmap='gray', vmin=0, vmax=255)

#ax.imshow(Z)
#plt.show()

